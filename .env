# Fallback env — environment-specific files take precedence:
#   .env.development  → used by `vite` (local dev)
#   .env.production   → used by `vite build` (production)
VITE_API_URL=http://localhost:8000/api
